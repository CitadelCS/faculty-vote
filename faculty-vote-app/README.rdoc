== README

Initial Setup:
===============
    bundle install

    rake db:migrate

    rake db:seed
 
    rake db:test:prepare

Run on Local (Cloud9):
======================
    rails s -p $PORT -b $IP